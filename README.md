# 🖐️ HandDrawTracker

A real-time hand drawing app using MediaPipe and OpenCV. Control your brush with your index finger, pick colors by hovering, and toggle eraser mode with a keypress.

## 🎨 Features
- Draw with your fingertip using webcam input
- Hover to pick colors from the top-left palette
- Press `E` to toggle eraser mode
- Press `C` to clear the canvas
- Press `Q` to quit

## 🚀 Getting Started

Install dependencies:

```bash
pip install -r requirements.txt
